const { default: expect } = require('expect');

const calculator = require('../calculator');

test('adds a + b', () => {
    //
})

test('substracts a - b', () => {
   //
})

test('multiplies a * b', () => {
    //
})

test('divides a / b', () => {
    // Consider decimals
})

test('Raises to power a ^ b', () => {
    //
})

